name: Deploy DocC to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-latest
    env:
      DEVELOPER_DIR: "/Applications/Xcode_26.0.app/Contents/Developer"
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v4

      - name: Resolve package dependencies
        run: swift package resolve

      - name: Build DocC documentation
        run: |
          xcodebuild docbuild \
            -scheme swift-mdlint-ja-Package \
            -destination "generic/platform=macOS" \
            -derivedDataPath DerivedData

          $(xcrun --find docc) merge DerivedData/Build/Products/Debug/MDLintCore.doccarchive DerivedData/Build/Products/Debug/MDLintRules.doccarchive DerivedData/Build/Products/Debug/MDLintConfig.doccarchive DerivedData/Build/Products/Debug/mdlint-ja.doccarchive \
            --synthesized-landing-page-name swift-mdlint-ja \
            --synthesized-landing-page-kind "Welcome To swift-mdlint-ja"  \
            --synthesized-landing-page-topics-style compactGrid \
            --output-path DerivedData/Build/Products/Debug/swift-mdlint-ja.doccarchive

          $(xcrun --find docc) process-archive \
             transform-for-static-hosting DerivedData/Build/Products/Debug/swift-mdlint-ja.doccarchive \
             --output-path ./docs \
             --hosting-base-path "swift-mdlint-ja"


      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
