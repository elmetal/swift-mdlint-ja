name: Swift Tests

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '*.md'
      - '**/*.md'
      - '.github/workflows/docc-pages.yml'
  pull_request:
    paths-ignore:
      - '*.md'
      - '**/*.md'
      - '.github/workflows/docc-pages.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: swift test
